function params = wrapMyParameters(params,x)
params.m_dot = x(1);
% params.res_length = x(2);
end

%%%%%%%%
%%%%%%%% Add parameter for spacing between side streams.
%%%%%%%% Range to be determined.
%%%%%%%%